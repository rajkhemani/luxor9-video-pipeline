apiVersion: v1
kind: Secret
metadata:
  name: mcp-secrets
  namespace: {{K8S_NAMESPACE}}
type: Opaque
data:
  # Base64 encoded values
  gemini-api-key: "{{BASE64_API_KEY}}"
  db-connection-string: "{{BASE64_DB_CONN}}"
